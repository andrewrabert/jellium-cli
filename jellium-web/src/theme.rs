//! The last of this client's own card measurements, which dissolve into
//! `card::Card` once that vocabulary lands.

pub const CARD_WIDTH: f32 = 176.0;

pub const RAIL_HEIGHT: f32 = 288.0;

/// A card's poster over its name and its subtitle, without the gutter the
/// layout adds around it.
pub const CARD_HEIGHT: f32 = CARD_WIDTH * 1.5 + 48.0;

pub const IMAGE_WIDTH: u16 = 352;
